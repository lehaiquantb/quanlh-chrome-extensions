import { notification } from "antd"
export class NotificationManager {
  static success = notification.success
  static error = notification.error
  static warning = notification.warning
  static info = notification.info
}
